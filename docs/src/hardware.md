# Hardware

Hardware work is maintained on the `hardware` branch.

It includes ESP32 firmware, perception code, controller scripts, and generated result artifacts from physical or hardware-oriented runs.
